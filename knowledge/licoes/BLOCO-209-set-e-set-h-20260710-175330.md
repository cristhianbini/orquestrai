---
tipo: licao-automatica
bloco: BLOCO-209
title: "Auto: set +e; set -H"
sha256: dbdfb9b2ecd17fbe2b891b9388ccf55c16f8a9fe681dc7737978a9746c0e7d6b
created: 2026-07-10T17:53:30.353Z
---

# BLOCO-209 - set +e; set -H

## O que ficou aprendido
Bloco LAVE persistido automaticamente para consulta futura.

## Script completo
~~~bash
set +e; set -H
cd /var/www/orquestrai/projects/cafe-real

echo "=== ESTRUTURA DO PROJETO ==="
tree -L 2 -a --dirsfirst 2>/dev/null || find . -maxdepth 2 -type d
echo ""

echo "=== CONFIGURAÇÃO (project.json) ==="
cat project.json 2>/dev/null | head -n 30
echo ""

echo "=== DOCUMENTAÇÃO DISPONÍVEL ==="
ls -lh docs/ 2>/dev/null
echo ""

echo "=== ÚLTIMO PLANO MAS ==="
ls -lt docs/plano-mas_*.md 2>/dev/null | head -n 1
ULTIMO=$(ls -t docs/plano-mas_*.md 2>/dev/null | head -n 1)
if [ -n "$ULTIMO" ]; then
    echo "--- PRIMEIRAS 25 LINHAS ---"
    head -n 25 "$ULTIMO"
fi
~~~
