---
tipo: licao-automatica
bloco: BLOCO-59
title: "Auto: set +e; set +H"
sha256: aa784af1d2ec6f8813bc71b97c3cf7b15386dfaf71c2453d5a371115e446bd12
created: 2026-06-27T02:42:10.550Z
---

# BLOCO-59 - set +e; set +H

## O que ficou aprendido
Bloco LAVE persistido automaticamente para consulta futura.

## Script completo
~~~bash
set +e; set +H
MARKER="MK_SCOUT_OBJ_$(date +%Y%m%d)"
REPORT="/tmp/scout_objetivo_$(date +%s).txt"
grep -q "$MARKER" /tmp/.markers 2>/dev/null && { echo "Bloco ja executado hoje"; exit 0; }

echo "===== SCOUT: Identificação de dados sensíveis =====" | tee "$REPORT"
echo "Timestamp: $(date '+%Y-%m-%d %H:%M:%S')" | tee -a "$REPORT"
echo "" | tee -a "$REPORT"

echo "[1] Arquivos .env em ambientes XMonex:" | tee -a "$REPORT"
find /var/www/xmonex* -type f -name ".env" -exec ls -lh {} \; 2>/dev/null | tee -a "$REPORT"
echo "" | tee -a "$REPORT"

echo "[2] Conteúdo DATABASE_ em .env (primeiras 3 linhas de cada):" | tee -a "$REPORT"
for ENV_FILE in /var/www/xmonex/backend/.env /var/www/xmonex_teste/backend/.env /var/www/xmonex_release/backend/.env; do
  if [ -f "$ENV_FILE" ]; then
    echo "--- $ENV_FILE ---" | tee -a "$REPORT"
    grep -E "^DATABASE_|^JWT_SECRET|^SESSION_SECRET" "$ENV_FILE" 2>/dev/null | head -3 | tee -a "$REPORT"
    echo "" | tee -a "$REPORT"
  fi
done

echo "[3] Logs recentes (últimas 5 linhas de error.log PM2):" | tee -a "$REPORT"
tail -5 ~/.pm2/logs/*error*.log 2>/dev/null | tee -a "$REPORT"
echo "" | tee -a "$REPORT"

echo "[4] Tabelas MySQL no schema xmonex (primeiras 10):" | tee -a "$REPORT"
mysql -h localhost -u root -p"$(grep DATABASE_PASSWORD /var/www/xmonex/backend/.env 2>/dev/null | cut -d= -f2)" -e "SHOW TABLES FROM xmonex;" 2>/dev/null | head -11 | tee -a "$REPORT"
echo "" | tee -a "$REPORT"

echo "[5] Estrutura tabela usuarios (se existir):" | tee -a "$REPORT"
mysql -h localhost -u root -p"$(grep DATABASE_PASSWORD /var/www/xmonex/backend/.env 2>/dev/null | cut -d= -f2)" -e "DESCRIBE xmonex.usuarios;" 2>/dev/null | tee -a "$REPORT"
echo "" | tee -a "$REPORT"

echo "[6] Cron jobs do usuário root:" | tee -a "$REPORT"
crontab -l 2>/dev/null | tee -a "$REPORT"
echo "" | tee -a "$REPORT"

echo "[7] Scripts em /usr/local/bin (últimas 10 modificações):" | tee -a "$REPORT"
find /usr/local/bin -type f -mtime -30 -ls 2>/dev/null | tail -10 | tee -a "$REPORT"
echo "" | tee -a "$REPORT"

echo "[8] Processos PM2 ativos:" | tee -a "$REPORT"
pm2 list 2>/dev/null | tee -a "$REPORT"
echo "" | tee -a "$REPORT"

echo "[9] Hash SHA256 dos .env (validação integridade):" | tee -a "$REPORT"
sha256sum /var/www/xmonex*/backend/.env 2>/dev/null | tee -a "$REPORT"
echo "" | tee -a "$REPORT"

echo "$MARKER" >> /tmp/.markers
echo "Relatório salvo: $REPORT"
cat "$REPORT"
echo "===== fim BLOCO-SCOUT-OBJ ====="
~~~
