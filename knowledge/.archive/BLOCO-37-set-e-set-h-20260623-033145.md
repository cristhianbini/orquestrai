---
tipo: licao-automatica
bloco: BLOCO-37
title: "Auto: set +e; set +H"
sha256: 9b3d9327dc53faa36f37e1b7a35fa201c6da688f6fa68bdd5e6ce21f137c10b2
created: 2026-06-23T03:31:45.657Z
---

# BLOCO-37 - set +e; set +H

## O que ficou aprendido
Bloco LAVE persistido automaticamente para consulta futura.

## Script completo
~~~bash
set +e; set +H
BACKEND_ROOT="/opt/xmonex/backend"
CONFIG_DIR="${BACKEND_ROOT}/config/env"
MIGRATION_DIR="${BACKEND_ROOT}/src/main/resources/db/migration"
REPO_DIR="${BACKEND_ROOT}/repository"
CICD_DIR="${BACKEND_ROOT}/.github/workflows"
REPORT_FILE="/tmp/xmonex_drift_audit_$(date +%s).txt"

echo "===== AUDITORIA DE DRIFT XMONEX BACKEND =====" | tee "${REPORT_FILE}"
echo "Timestamp: $(date -Iseconds)" | tee -a "${REPORT_FILE}"
echo "" | tee -a "${REPORT_FILE}"

echo "[1] COMPARACAO DE CONFIGURACOES ENTRE RELEASE E PROD" | tee -a "${REPORT_FILE}"
if [ -f "${CONFIG_DIR}/production.yml" ] && [ -f "${CONFIG_DIR}/release.yml" ]; then
  diff -u "${CONFIG_DIR}/release.yml" "${CONFIG_DIR}/production.yml" | tee -a "${REPORT_FILE}"
  echo "Linhas divergentes: $(diff "${CONFIG_DIR}/release.yml" "${CONFIG_DIR}/production.yml" | wc -l)" | tee -a "${REPORT_FILE}"
else
  echo "AVISO: Arquivos de config nao encontrados" | tee -a "${REPORT_FILE}"
fi
echo "" | tee -a "${REPORT_FILE}"

echo "[2] ANALISE DE SCRIPTS DE MIGRACAO DE BANCO" | tee -a "${REPORT_FILE}"
if [ -d "${MIGRATION_DIR}" ]; then
  cd "${BACKEND_ROOT}" 2>/dev/null || echo "Diretorio backend nao existe"
  git diff release..prod -- "${MIGRATION_DIR}" 2>/dev/null | tee -a "${REPORT_FILE}"
  echo "Migracoes divergentes: $(git diff release..prod --name-only -- "${MIGRATION_DIR}" 2>/dev/null | wc -l)" | tee -a "${REPORT_FILE}"
  ls -lht "${MIGRATION_DIR}"/V*.sql 2>/dev/null | head -5 | tee -a "${REPORT_FILE}"
else
  echo "AVISO: Diretorio de migracoes nao encontrado" | tee -a "${REPORT_FILE}"
fi
echo "" | tee -a "${REPORT_FILE}"

echo "[3] VERIFICACAO DE REPOSITORIOS JPA" | tee -a "${REPORT_FILE}"
if [ -d "${REPO_DIR}" ]; then
  git diff release..prod -- "${REPO_DIR}/OrderRepository.java" "${REPO_DIR}/CustomerRepository.java" 2>/dev/null | tee -a "${REPORT_FILE}"
  grep -n "@Query" "${REPO_DIR}"/*Repository.java 2>/dev/null | tee -a "${REPORT_FILE}"
else
  echo "AVISO: Diretorio de repositorios nao encontrado" | tee -a "${REPORT_FILE}"
fi
echo "" | tee -a "${REPORT_FILE}"

echo "[4] AUDITORIA DE CI/CD E DOCKERFILE" | tee -a "${REPORT_FILE}"
if [ -f "${CICD_DIR}/deploy.yml" ]; then
  git diff release..prod -- "${CICD_DIR}/deploy.yml" 2>/dev/null | tee -a "${REPORT_FILE}"
else
  echo "AVISO: Workflow deploy.yml nao encontrado" | tee -a "${REPORT_FILE}"
fi
if [ -f "${BACKEND_ROOT}/Dockerfile.prod" ]; then
  git diff release..prod -- "${BACKEND_ROOT}/Dockerfile.prod" 2>/dev/null | tee -a "${REPORT_FILE}"
  grep -E "^(FROM|ENV|RUN)" "${BACKEND_ROOT}/Dockerfile.prod" | tee -a "${REPORT_FILE}"
else
  echo "AVISO: Dockerfile.prod nao encontrado" | tee -a "${REPORT_FILE}"
fi
echo "" | tee -a "${REPORT_FILE}"

echo "[5] RESUMO DE COMMITS ENTRE release E prod" | tee -a "${REPORT_FILE}"
cd "${BACKEND_ROOT}" 2>/dev/null && git log release..prod --oneline --no-merges | head -20 | tee -a "${REPORT_FILE}"
echo "" | tee -a "${REPORT_FILE}"

echo "Relatorio salvo em: ${REPORT_FILE}" | tee -a "${REPORT_FILE}"
echo "===== fim BLOCO-DRIFT-AUDIT ====="
~~~
