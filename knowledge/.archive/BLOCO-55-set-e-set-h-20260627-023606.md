---
tipo: licao-automatica
bloco: BLOCO-55
title: "Auto: set +e; set +H"
sha256: c3d4ecbd1a00f6dc06cd08b488456b97f7a2c56a705322b0533d1dae55512908
created: 2026-06-27T02:36:06.306Z
---

# BLOCO-55 - set +e; set +H

## O que ficou aprendido
Bloco LAVE persistido automaticamente para consulta futura.

## Script completo
~~~bash
set +e; set +H
REPORT=/tmp/upload_deep_dive_$(date +%s).txt
PROD_BE=/var/www/xmonex/backend
TESTE_BE=/var/www/xmonex_teste/backend
RELEASE_BE=/var/www/xmonex_release/backend
PROD_FE=/var/www/xmonex/frontend
TESTE_FE=/var/www/xmonex_teste/frontend
RELEASE_FE=/var/www/xmonex_release/frontend

echo "===== BLOCO-UPLOAD-DEEP-DIVE =====" | tee $REPORT
echo "[1] BUSCA RECURSIVA POR REFERÊNCIAS A 'uploads' NO CÓDIGO BACKEND" | tee -a $REPORT
for BE_DIR in $PROD_BE $TESTE_BE $RELEASE_BE; do
    if [ -d "$BE_DIR" ]; then
        echo ">>> $BE_DIR" | tee -a $REPORT
        grep -rn "uploads" "$BE_DIR"/*.js "$BE_DIR"/routes/*.js "$BE_DIR"/controllers/*.js 2>/dev/null | head -20 | tee -a $REPORT
    fi
done

echo "" | tee -a $REPORT
echo "[2] BUSCA RECURSIVA POR REFERÊNCIAS A 'multer' OU 'formidable' (libs de upload)" | tee -a $REPORT
for BE_DIR in $PROD_BE $TESTE_BE $RELEASE_BE; do
    if [ -d "$BE_DIR" ]; then
        echo ">>> $BE_DIR" | tee -a $REPORT
        grep -rn -E "(multer|formidable|busboy)" "$BE_DIR"/*.js "$BE_DIR"/routes/*.js "$BE_DIR"/controllers/*.js "$BE_DIR"/middlewares/*.js 2>/dev/null | tee -a $REPORT
    fi
done

echo "" | tee -a $REPORT
echo "[3] VERIFICAÇÃO /etc/nginx/nginx.conf (configurações globais)" | tee -a $REPORT
if [ -f /etc/nginx/nginx.conf ]; then
    grep -n "client_max_body_size" /etc/nginx/nginx.conf 2>/dev/null | tee -a $REPORT
    echo ">>> Bloco http completo:" | tee -a $REPORT
    sed -n '/^http {/,/^}/p' /etc/nginx/nginx.conf 2>/dev/null | head -30 | tee -a $REPORT
else
    echo ">>> /etc/nginx/nginx.conf (NOT FOUND)" | tee -a $REPORT
fi

echo "" | tee -a $REPORT
echo "[4] VERIFICAÇÃO /etc/nginx/sites-available/ (todos os arquivos)" | tee -a $REPORT
ls -lh /etc/nginx/sites-available/ 2>/dev/null | tee -a $REPORT
for SITE_CONF in /etc/nginx/sites-available/*; do
    if [ -f "$SITE_CONF" ]; then
        echo ">>> $SITE_CONF" | tee -a $REPORT
        grep -n "client_max_body_size" "$SITE_CONF" 2>/dev/null | tee -a $REPORT
    fi
done

echo "" | tee -a $REPORT
echo "[5] PACKAGE.JSON - DEPENDÊNCIAS DE UPLOAD" | tee -a $REPORT
for PKG in $PROD_BE/package.json $TESTE_BE/package.json $RELEASE_BE/package.json; do
    if [ -f "$PKG" ]; then
        echo ">>> $PKG" | tee -a $REPORT
        grep -E "(multer|formidable|busboy|express-fileupload)" "$PKG" 2>/dev/null | tee -a $REPORT
    fi
done

echo "" | tee -a $REPORT
echo "Relatório salvo em: $REPORT"
echo "===== FIM BLOCO-UPLOAD-DEEP-DIVE ====="
~~~
