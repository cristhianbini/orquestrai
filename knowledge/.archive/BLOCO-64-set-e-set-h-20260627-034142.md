---
tipo: licao-automatica
bloco: BLOCO-64
title: "Auto: set +e; set +H"
sha256: 2f35e4d86036eb247d0c9c1c4f296fe5f5acc759dbf1fec4e12eb08f290f03c0
created: 2026-06-27T03:41:42.838Z
---

# BLOCO-64 - set +e; set +H

## O que ficou aprendido
Bloco LAVE persistido automaticamente para consulta futura.

## Script completo
~~~bash
set +e; set +H
REPORT=/tmp/mutationobserver_audit_$(date +%s).txt
FRONTEND_PROD=/var/www/xmonex/frontend
FRONTEND_TESTE=/var/www/xmonex_teste/frontend
FRONTEND_RELEASE=/var/www/xmonex_release/frontend

echo "=== AUDITORIA: MutationObserver com callback que adiciona/remove nós ===" | tee "$REPORT"
echo "Data: $(date '+%Y-%m-%d %H:%M:%S')" | tee -a "$REPORT"
echo "" | tee -a "$REPORT"

echo "[1] Buscando MutationObserver em código frontend (PROD/TESTE/RELEASE):" | tee -a "$REPORT"
for ENV_PATH in "$FRONTEND_PROD" "$FRONTEND_TESTE" "$FRONTEND_RELEASE"; do
  [ -d "$ENV_PATH" ] || continue
  echo "  Ambiente: $ENV_PATH" | tee -a "$REPORT"
  grep -rn "new MutationObserver\|MutationObserver(" "$ENV_PATH/src" --include="*.js" --include="*.jsx" --include="*.ts" --include="*.tsx" 2>/dev/null | head -20 | tee -a "$REPORT"
done
echo "" | tee -a "$REPORT"

echo "[2] Verificando callbacks que modificam DOM (appendChild, removeChild, insertBefore):" | tee -a "$REPORT"
for ENV_PATH in "$FRONTEND_PROD" "$FRONTEND_TESTE" "$FRONTEND_RELEASE"; do
  [ -d "$ENV_PATH" ] || continue
  echo "  Ambiente: $ENV_PATH" | tee -a "$REPORT"
  grep -rn "\.appendChild\|\.removeChild\|\.insertBefore\|\.remove()" "$ENV_PATH/src" --include="*.js" --include="*.jsx" --include="*.ts" --include="*.tsx" 2>/dev/null | grep -i "mutation\|observer" | head -15 | tee -a "$REPORT"
done
echo "" | tee -a "$REPORT"

echo "[3] Buscando configurações subtree:true em MutationObserver:" | tee -a "$REPORT"
for ENV_PATH in "$FRONTEND_PROD" "$FRONTEND_TESTE" "$FRONTEND_RELEASE"; do
  [ -d "$ENV_PATH" ] || continue
  echo "  Ambiente: $ENV_PATH" | tee -a "$REPORT"
  grep -rn "subtree.*true\|childList.*true" "$ENV_PATH/src" --include="*.js" --include="*.jsx" --include="*.ts" --include="*.tsx" 2>/dev/null | head -15 | tee -a "$REPORT"
done
echo "" | tee -a "$REPORT"

echo "[4] DIAGNÓSTICO - Riscos identificados:" | tee -a "$REPORT"
echo "  • Loop infinito: callback que adiciona/remove nós dispara novas mutações" | tee -a "$REPORT"
echo "  • Performance: subtree:true em document.body gera alto volume de registros" | tee -a "$REPORT"
echo "  • Mitigações sugeridas:" | tee -a "$REPORT"
echo "    - Usar observer.disconnect() antes de modificar DOM no callback" | tee -a "$REPORT"
echo "    - Reconectar observer.observe() após modificações" | tee -a "$REPORT"
echo "    - Limitar escopo com attributeFilter ou targetNode específico" | tee -a "$REPORT"
echo "    - Implementar debouncing/throttling no callback" | tee -a "$REPORT"
echo "" | tee -a "$REPORT"

echo "[5] Relatório salvo em: $REPORT" | tee -a "$REPORT"
wc -l "$REPORT" | tee -a "$REPORT"
echo "===== fim BLOCO-MUTATIONOBSERVER-AUDIT ====="
~~~
