---
tipo: licao-automatica
bloco: BLOCO-143
title: "Auto: set +e; set +H"
sha256: 98528fe37b6799d416f2401f27e877a4188a2272d448a591ab7bb4849de043ed
created: 2026-06-29T06:52:19.614Z
---

# BLOCO-143 - set +e; set +H

## O que ficou aprendido
Bloco LAVE persistido automaticamente para consulta futura.

## Script completo
~~~bash
set +e; set +H
echo "### VAULT STRUCTURE ###"
ls -la /var/www/orquestrai/knowledge/
echo -e "\n### CONTEXTOS ###"
ls -lh /var/www/orquestrai/knowledge/contextos/ 2>/dev/null || echo "Dir vazio ou inexistente"
echo -e "\n### LICOES ###"
ls -lh /var/www/orquestrai/knowledge/licoes/ 2>/dev/null || echo "Dir vazio ou inexistente"
echo -e "\n### AUDIT BLOCKS (BLOCOS) ###"
ls -lth /var/www/orquestrai/data/knowledge/blocos/ | head -5
echo -e "\n### LATEST BLOCK CONTENT (HEAD) ###"
LATEST=$(ls -t /var/www/orquestrai/data/knowledge/blocos/ 2>/dev/null | head -1)
if [ -n "$LATEST" ]; then
    head -20 "/var/www/orquestrai/data/knowledge/blocos/$LATEST"
else
    echo "Nenhum bloco encontrado."
fi
~~~
